[project]
name = "pcp-solvers"
version = "0.1.0"
description = "Solvers for the Partition Coloring Problem"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ortools>=9.8",
]

[project.scripts]
pcp-solve = "src.pcp.runner:main"
